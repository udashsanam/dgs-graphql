type Query {
    loloma: [ShowCategory]
    search(searchFilter: SearchFilter):[Show]
}

type ShowCategory {
    id: Int
    name: String
    shows: [Show]
}

type Show {
    title: String
    artworkUrl:String
}

input SearchFilter {
    title: String
}